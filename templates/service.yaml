apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-configuration-helm-my-demo-app.fullname" . }}
  labels:
    {{- include "platform-configuration-helm-my-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platform-configuration-helm-my-demo-app.selectorLabels" . | nindent 4 }}
